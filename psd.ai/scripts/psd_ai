#!/usr/bin/env python3
"""psd_ai — one entrypoint for every psd_ai-* CLI.

Discovers `scripts/psd_ai-<name>` siblings and dispatches to them, the
same way `git` finds `git-foo`. Pure passthrough: any flags after the
subcommand name are forwarded as-is, so `psd_ai mail list --pretty`
behaves exactly like `psd_ai-mail list --pretty`.

    psd_ai                       # list every subcommand + short help
    psd_ai <name>                # run psd_ai-<name>
    psd_ai <name> <args...>      # run psd_ai-<name> <args...>
    psd_ai help                  # alias for `psd_ai` (the listing)
    psd_ai help <name>           # show the subcommand's own --help
    psd_ai --version             # print version

Designed to live in `scripts/`. Symlink it onto `$PATH` (`ln -s
$PWD/scripts/psd_ai ~/.local/bin/psd_ai`) to use it without typing
the full path.
"""
from __future__ import annotations

import os
import re
import subprocess
import sys
from pathlib import Path

VERSION = "0.1.0"

SCRIPTS_DIR = Path(__file__).resolve().parent


def _list_subcommands() -> list[Path]:
    """Find every executable `psd_ai-*` next to this file (excluding
    the dispatcher itself + any .bak / .pre-* migration leftovers)."""
    out = []
    for p in sorted(SCRIPTS_DIR.iterdir()):
        if not p.is_file():
            continue
        if p.name == "psd_ai":
            continue
        if not p.name.startswith("psd_ai-"):
            continue
        if any(p.name.endswith(suf) for suf in (".bak", ".pre-cli-refactor", ".pyc")):
            continue
        if not os.access(p, os.X_OK):
            continue
        out.append(p)
    return out


def _short_help(path: Path) -> str:
    """Pull the first non-blank line of the docstring (after the shebang)
    as a 1-line description. Falls back to '(no description)'."""
    try:
        text = path.read_text()
    except Exception:
        return "(unreadable)"
    m = re.search(r'"""(.*?)"""', text, re.DOTALL)
    if not m:
        return ""
    body = m.group(1).strip()
    if not body:
        return ""
    # First non-blank line, strip the leading "name — " noise.
    first = next((ln for ln in body.splitlines() if ln.strip()), "")
    first = re.sub(r"^psd_ai-\w+\s*—\s*", "", first).strip()
    return first


def _is_runnable_subcommand(path: Path) -> bool:
    return path.exists() and path.is_file() and os.access(path, os.X_OK)


def _print_listing() -> None:
    """`psd_ai` with no args (or `psd_ai help`) — print the table."""
    sys.stdout.write(f"psd_ai {VERSION} — every feature, on the shell.\n\n")
    sys.stdout.write("Usage:  psd_ai <subcommand> [args...]\n\n")
    sys.stdout.write("Available subcommands:\n")
    subs = _list_subcommands()
    width = max((len(p.name[len("psd_ai-"):]) for p in subs), default=10)
    for p in subs:
        name = p.name[len("psd_ai-"):]
        desc = _short_help(p)
        sys.stdout.write(f"  {name:<{width + 2}}{desc}\n")
    sys.stdout.write(
        "\nRun `psd_ai help <subcommand>` for that tool's own --help.\n"
    )


def main(argv: list[str] | None = None) -> int:
    argv = list(sys.argv[1:] if argv is None else argv)

    if not argv or argv[0] in ("-h", "--help", "help") and len(argv) == 1:
        _print_listing()
        return 0

    if argv[0] in ("-v", "--version"):
        sys.stdout.write(f"psd_ai {VERSION}\n")
        return 0

    if argv[0] == "help":
        # `psd_ai help foo` → `psd_ai-foo --help`
        if len(argv) < 2:
            _print_listing()
            return 0
        sub = SCRIPTS_DIR / f"psd_ai-{argv[1]}"
        if not _is_runnable_subcommand(sub):
            sys.stderr.write(f"psd_ai: unknown subcommand {argv[1]!r}\n")
            return 1
        return subprocess.call([str(sub), "--help"])

    # `psd_ai foo ...` → exec `psd_ai-foo ...` under the project venv.
    name = argv[0]
    sub = SCRIPTS_DIR / f"psd_ai-{name}"
    if not _is_runnable_subcommand(sub):
        sys.stderr.write(
            f"psd_ai: unknown subcommand {name!r}. "
            f"Try `psd_ai help` to see available ones.\n"
        )
        return 1
    # Run the subcommand with the project's venv Python so dependencies
    # (bcrypt, sqlalchemy, fastapi, ...) resolve. Fall back to whatever
    # `python` is on PATH if no venv is found — useful in container
    # installs that wire the deps system-wide.
    repo_root = SCRIPTS_DIR.parent
    venv_python = repo_root / "venv" / "bin" / "python"
    py = str(venv_python) if venv_python.exists() else sys.executable
    os.execv(py, [py, str(sub)] + argv[1:])
    return 0  # unreachable


if __name__ == "__main__":
    sys.exit(main())
